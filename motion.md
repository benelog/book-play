# 작업 지시서 — 어린 왕자 영화를 모션 코믹(2.5D)으로 (2026-09-26 작성, 같은 날 검토 반영)

지금 영화(`books/little-prince/film/`)는 그림 판 위를 카메라가 날아다니고, 말하는 인물 쪽으로 다가갑니다(`shots.js`).
여기에 **깊이별 층의 시차, 눈 깜빡임, 대사에 맞춘 입 모양, 단어 단위 자막 강조, 목도리 흔들림**을 더해 움직이는 그림책으로 만듭니다.
새 장면을 그리지 않습니다. 이미 있는 그림을 **쪼개고 조금씩 바꾼 사본**만 만들어 인물 일관성을 지킵니다.

원화 애니메이션, AI 이미지→영상, Live2D·Spine 리깅은 검토 후 제외했습니다(분량, 인물 흔들림, `file://`에서 WebGL 텍스처 불가).
예외로 인물이 작거나 없는 몇 장면만 짧은 영상 반복을 쓸 수 있습니다(7단계).

## 검토에서 바뀐 것 (2026-09-26)

처음 계획을 `film.js`·`film.css`·`film-voices.py`와 2장 그림에 대조해 검토했고, 아래를 바꿨습니다.

- **시차를 `translateZ`가 아니라 2D `translate`로 냅니다.** 카메라가 `translateZ(1000 - 1000/s)`라서 확대 s=2인 클로즈업에서는 눈과 판 사이가 500px뿐입니다.
  z=30인 층은 6% 커져 보여 발이 땅에서 뜨고, 반면 카메라 좌우 흔들림은 확대 상태에서 1~2px이라 시차가 안 보입니다.
  `frame()`에서 카메라가 판 중심에서 벗어난 양에 비례해 층을 옮기면 정확하고, 정지 상태에서는 원본과 픽셀 단위로 일치합니다.
- **층은 "인물 vs 나머지"가 아니라 깊이면(하늘·먼 산 / 땅+인물 / 앞쪽 소품)으로 나눕니다.** chapter-02에서 조종사는 비행기 동체 위에, 어린 왕자는 날개와 지평선 위에 있어 인물 뒤를 채우려면 비행기를 새로 그려야 합니다.
  지평선에서 자르면 채울 것은 하늘 조각뿐이고, 발이 땅과 함께 움직여 접지가 깨지지 않습니다.
- **깜빡임·입 조각은 인물 층 없이 원본 `<img>` 위에 얹습니다.** 그래서 "말하는 얼굴 + 하늘/땅 시차"가 최소 시제품이고, 인물 잘라 내기는 숨쉬기·흔들림에만 필요하니 뒤(5단계)로 미룹니다.
- **흔들림은 `feTurbulence` 대신 목도리를 별도 층으로 떼어 `rotate`합니다.** SVG 필터는 매 프레임 층 전체를 다시 래스터화해 모바일에서 무겁고, 필터를 건 요소의 자손은 3D가 평면화됩니다.
- **입은 2단계가 아니라 3단계(닫힘·반·열림)**, 음량 곡선은 구간 목록 대신 50ms마다 한 글자로 적은 문자열.
- **추가**: 단어 타임스탬프로 자막 단어 강조, 얼굴 편집은 머리 주변 crop으로, 클로즈업용 2배 업스케일, 먼 층 정적 blur, 자동 대조 도구.

## 지켜야 할 제약 (CLAUDE.md)

- **`file://`에서 동작.** `fetch()` 금지, ES 모듈 금지, WebGL 금지(로컬 이미지는 교차 출처라 텍스처가 안 됨).
  CSS 3D, SVG 필터, `<canvas>` 2D의 `drawImage`는 됩니다(픽셀 읽기 `getImageData`만 막힘). `<video>`와 `<audio>` 재생도 됩니다.
  WebAudio `AnalyserNode`로 로컬 mp3를 분석하면 `file://`에서는 0만 나옵니다. **입 모양과 단어 시각은 미리 계산해 둡니다**(1단계).
- 그림 판에 직각인 면을 넣지 않습니다(Chrome이 레이어를 쪼갬). 층은 **평행하게** 겹칩니다. 판 사이 z 차이는 작게 둡니다.
- `.panel`은 `preserve-3d`라 `overflow: hidden`을 걸면 자손이 평면화됩니다. 층은 판 bbox 안에서만 자릅니다.
- 얼굴 조각은 그것이 덮는 그림(원본 `<img>` 또는 인물 층)과 **같은 평면의 자식**으로 둡니다. 다른 평면이면 카메라가 돌 때 어긋납니다.
- `prefers-reduced-motion`이면 지금처럼 정지 그림만 보여 줍니다(`reduced` 변수). 자막 단어 강조는 움직임이 아니니 그대로 둡니다.
- 앱 껍데기(`index.html`·`css/`·`js/`)를 바꾸면 `sw.js`의 `VERSION`을 올립니다. 영화 폴더만 바꾸면 필요 없습니다.
- 애니메이션 확인은 headless Chrome + DevTools 프로토콜 스크린숏으로 합니다(이 컴퓨터의 Chrome 확장 탭에서는 `requestAnimationFrame`이 돌지 않음).
- 그림 편집은 Codex CLI로 합니다. 방법은 `books/little-prince/images/README.md`의 "기준 이미지에 맞춘 다시 그리기"에 있습니다.
  다만 이 작업에서는 그림 전체가 아니라 **crop을 편집**합니다(3·5단계). 전체 그림을 줄 때만 4:3→3:2 거울 띠와 ORB+RANSAC 정렬이 필요합니다.
  OpenCV는 `uv run --with opencv-python-headless --with numpy`로 씁니다.

## 선행 조건

`next.md`의 녹음 작업(`film/audio.js`, `film/audio/*.mp3`, `tools/film-voices.py`)이 아직 커밋 전입니다.
`audio.js`는 지금 키를 공백으로 이은 문자열인데, 입 곡선과 단어 시각을 넣으려면 키별 객체로 바꿔야 합니다.
**녹음을 먼저 마무리해 커밋한 뒤** 이 작업을 시작합니다.

## 만들 파일

```
books/little-prince/images/layers/<id>/
  far.jpg           하늘·먼 산 등 지평선 위쪽 (1200x900, 지평선 아래는 버려도 됨)
  mid.png           땅·소품·인물 (투명 배경, 지평선 위는 투명; 인물 머리 뒤 하늘만 채워 넣음)
  near.png          (있을 때만) 앞쪽 소품 — 종이, 재킷, 풀
  <who>-face.png    얼굴 스프라이트: 눈 감음 | 입 반 | 입 열림 (얼굴 bbox 크기 x3, 가로로 이어 붙임)
  <who>.png         (5단계) 인물 층 — 숨쉬기용, 인물 bbox만
  <who>-scarf.png   (5단계) 목도리 꼬리 층 — 흔들림용
books/little-prince/film/layers.js   window.LP_FILM_LAYERS = { '<id>': { layers: [...], faces: {...} } }
books/little-prince/film/timing.js   window.LP_FILM_TIMING = { '<key>': { mouth: '0013688874...', words: [[ms, ms], ...] } }
books/little-prince/film/motion.js   층 배치와 시차, 깜빡임, 입, 자막 단어 강조, 흔들림
tools/film-layers.py                 층과 얼굴 스프라이트를 만드는 도구 (2·3·5단계)
tools/film-timing.py                 음량 곡선과 단어 시각을 만드는 도구 (1단계)
tools/film-motion-check.py           자동 대조 도구 (6단계)
```

`layers.js`에는 층마다 `x, y, w, h`(0~1 비율)와 시차 계수 `k`(far 0.6, mid 1.0, near 1.15쯤), 얼굴마다 bbox와 스프라이트 파일을 적습니다.
층 파일은 bbox만 WebP(알파) 또는 PNG로 저장하고, 그림 한 장에 1~2 MB를 넘기지 않게 합니다.
오프라인 저장(`js/pwa.js`)에 층 파일도 넣어야 하는지는 시제품 뒤에 정합니다.

## 단계

### 0. 시제품 범위

**2장 그림 전부(chapter-02, 02-1 ~ 02-5, 02-f1 ~ 02-f3)** 로 먼저 1~4단계를 끝까지 만듭니다.
2장은 조종사와 어린 왕자가 둘 다 나오고, 대사가 많고, 지평선이 뚜렷해 깊이 층 나누기가 쉽습니다.
사용자에게 보여 주고 방향을 확인한 뒤 5단계와 나머지 장으로 넓힙니다.

### 1. 입 곡선과 단어 시각 (`tools/film-timing.py` → `film/timing.js`)

- **음량 곡선**: mp3를 ffmpeg로 PCM(24kHz mono)으로 디코드해 50ms 창 RMS를 구하고, 줄 안의 최댓값으로 정규화해 0~9 한 글자씩 문자열로 적습니다(`mouth`).
  10초 줄이 200자입니다. 새로 녹음할 때는 `film-voices.py`가 이미 PCM을 메모리에 갖고 있으니 인코딩 전에 같은 함수로 계산합니다.
- **단어 시각**: `uv run --with faster-whisper`로 각 mp3를 정렬해(대본 텍스트를 `initial_prompt`로 주면 정확도가 오름) 단어별 `[시작ms, 끝ms]`를 적습니다(`words`).
  단어 수가 대본과 다르면 가까운 것끼리 맞추고, 못 맞추면 `words`를 비웁니다(자막 강조만 안 됨).
  이 시각으로 말하는 구간과 쉼을 알 수 있어 입 곡선의 문턱값보다 정확하고, 나중에 자막 단어 클릭 → `js/dict.js` 팝오버로 잇는 데도 씁니다.
- `audio.js` 형식은 키 문자열에서 키별 객체로 바꾸고 `film.js`의 `clipOf()`를 맞춥니다. `timing.js`는 따로 두어 `audio.js`는 녹음 도구만, `timing.js`는 타이밍 도구만 씁니다.
- 브라우저 음성(녹음이 없을 때)은 `SpeechSynthesisUtterance`의 `onboundary`(단어 경계)마다 입을 120ms 열고 그 단어를 강조합니다.

### 2. 깊이 층 나누기 (`far` / `mid` / `near`)

- 그림마다 지평선(또는 실내라면 벽과 바닥 경계)을 찾아 `far.jpg`와 `mid.png`로 나눕니다. 지평선은 `tools/film-layers.py`에 그림별 y(0~1)와 필요하면 꺾인 점 몇 개로 적습니다.
- 인물·비행기·나무처럼 지평선을 가로지르는 것은 **`mid`에 통째로** 둡니다. 그래서 `mid`의 마스크는 "지평선 아래 + 지평선 위로 솟은 것"이고, 뒤에 채울 것은 그 솟은 부분 뒤의 하늘뿐입니다.
  솟은 부분은 SAM(`segment-anything`, `uv`로 실행)이나 `rembg`로 잡고, 마스크 가장자리는 2~3px 부드럽게 합니다. 머리칼 삐침·목도리 끝이 잘리지 않았는지 눈으로 봅니다.
- `far.jpg`의 빈 하늘은 Codex에게 **그 부분 주변 crop**을 주고 "이 인물(물체)을 지우고 하늘을 이어서 채워라, 나머지는 바꾸지 마라"로 채웁니다. 마스크를 20px 넓힌 안쪽만 새 그림으로 바꾸고 밖은 원본 픽셀을 그대로 씁니다.
  하늘은 대개 그러데이션이라 crop이 작아도 되고, 정렬은 crop을 그대로 되붙이면 됩니다.
- 종이·재킷·풀처럼 화면 아래쪽 가장자리에 있는 것은 `near.png`로 뗄 수 있습니다(선택). 인물과 겹치면 떼지 않습니다.
- 인물이 없고 하늘도 없는 그림(실내 클로즈업 등)은 층을 나누지 않습니다.

### 3. 얼굴 스프라이트 (눈 감음, 입 반, 입 열림)

- 인물마다 머리 주변을 넉넉히(얼굴의 2배쯤) 잘라 512px 안팎으로 키운 crop을 Codex에 줍니다. 세 번 시킵니다: "눈만 감게", "입만 말하듯 조금 벌리게", "입만 더 벌리게(모음)". 나머지는 그대로.
  전체 그림을 주지 않는 이유: 모델이 나머지를 흔들 여지가 없고, 되붙일 때 crop 위치에 그대로 넣으면 되어 정렬이 필요 없습니다.
- 결과를 원래 크기로 줄여 **얼굴 bbox만** 잘라 `<who>-face.png`에 가로로 세 칸 이어 붙입니다. 눈·입 밖에서 원본과 차이가 나면(6단계 도구가 잡음) 버리고 다시 만듭니다.
- 원래 눈을 감았거나 얼굴이 안 보이는 그림(20-1, 26-1, 뒷모습)은 건너뜁니다. 양·비행기·종이 위 그림은 움직이지 않습니다.
- **클로즈업 선명도**: 지금도 cover×1.4면 1200px 그림을 2.5배로 확대해서 보여 줍니다. 얼굴 스프라이트(와 5단계의 인물 층)만 애니메이션 계열 업스케일러(Real-ESRGAN anime 모델 등)로 2배 키워 저장하면 클로즈업이 또렷해집니다.
  bbox만이라 용량은 감당됩니다. 원본 `<img>`와 경계가 보이면 조각 가장자리를 4px 페더링합니다.

### 4. 움직임 (`motion.js`)

- **시차**: `ensurePanel()`이 `<img>` 하나를 넣는 자리에, `layers.js`에 층이 있으면 `far`·`mid`·`near`를 같은 평면에 겹쳐 넣습니다(`translateZ` 없음).
  `frame()`에서 카메라가 판 중심에서 벗어난 양 `(cam.x - st.x, cam.y - st.y)`에 `(1 - k)`를 곱해 층을 `translate`합니다. 정지하고 판 중앙을 볼 때는 원본과 같습니다.
  카메라가 움직여야 시차가 보이니, 대사 동안 카메라를 천천히 옆으로 흘립니다: 줄마다 목표점을 ±40 세계 px 옮겨 `aim()`하고, 긴 나레이션은 문단 안에서 한 방향으로 계속 흐르게 합니다.
- **먼 층 흐림**: 클로즈업(scale ≥ cover)일 때 `far`에 `filter: blur(1.5px)`를 겁니다. 값이 바뀌지 않으면 다시 래스터화하지 않아 비용이 없고, 깊이감이 커집니다. 넓은 샷으로 돌아오면 뗍니다.
- **깜빡임**: 3~6초마다 무작위로 눈 감은 칸을 보여 줍니다. 50ms 크로스페이드로 감고 뜨고, 감은 채 80ms. 컷 직후 500ms는 깜빡이지 않고, 10번에 1번은 두 번 연속. 인물마다 따로 돕니다.
- **입**: 말하는 인물(`step.who`)의 `mouth` 문자열을 `playClip()`의 `a.currentTime`(재생 속도와 무관하게 음성 시각)으로 읽어 0~3 → 닫힘, 4~6 → 반, 7~9 → 열림을 고릅니다. 상태는 최소 80ms 유지합니다.
  `words`가 있으면 단어 사이 쉼에서는 닫습니다.
- **자막 단어 강조**: `renderSubtitle()`이 단어마다 `<span>`으로 나누고, `words`의 시각에 맞춰 지금 단어에 클래스를 줍니다. 브라우저 음성은 `onboundary`로 같은 일을 합니다. `reduced`여도 켭니다.
- 층이 없는 그림은 지금처럼 `<img>` 하나로 보여 줍니다. 층 파일을 못 불러오면(`onerror`) 원본으로 되돌립니다.
- `LP_FILM.motion.seek(ms)`(줄 안 시각 고정)와 `?motion=0`(끄기)을 두어 스크린숏이 재현되고 원본과 비교할 수 있게 합니다.

### 5. 인물 층: 숨쉬기와 목도리 (방향 확인 뒤)

- 인물을 `<who>.png`로 잘라 `mid` 위에 겹칩니다. 잘린 자리 뒤는 인물 주변 crop을 Codex로 채웁니다(2단계와 같은 방법, 마스크 20px 안쪽만 교체).
  인물끼리 겹치면(07-f4 안아 주는 장면) 한 층으로 둡니다. 다른 인물·소품과 닿아 있는 인물(종이에 손을 얹은 조종사)은 잘라도 숨쉬기를 주지 않습니다.
- **숨쉬기**: `scaleY(1 → 1.004)` 4초 주기, 발끝 기준(`transform-origin: bottom`). 0.6%는 접지가 벌어져 보이니 0.4%로 시작합니다.
- **목도리**: 꼬리 부분을 `<who>-scarf.png`로 떼어 목 위치(`layers.js`의 `pivot`)를 축으로 `rotate(-2deg ~ 2deg)` + 약한 `skewX`를 6~9초 주기 CSS keyframe으로 줍니다. 변형만이라 GPU 합성이고 다시 래스터화하지 않습니다.
  떼어 낸 자리 뒤도 crop으로 채웁니다. 머리칼·풀은 두어도 됩니다. 너무 흔들리면 그림이 녹아 보이니 각도를 작게 둡니다.

### 6. 자동 대조 도구 (`tools/film-motion-check.py`)

8단계에서 100장 넘는 그림 × 편집 3~5회를 눈으로 다 볼 수 없어 먼저 만듭니다.

- headless Chrome으로 `film/index.html`을 `file://`로 열어 그림마다 4프레임을 찍습니다: `?motion=0` 원본, 층을 겹친 정지 상태, 눈 감음, 입 열림(`seek`로 고정).
- 정지 상태와 원본의 차이가 문턱을 넘으면(층 경계 후광, 채운 하늘 색 차이) 표시합니다. 눈 감음·입 열림은 얼굴 bbox 밖의 차이가 넘으면 표시합니다.
- 결과를 contact sheet 한 장(원본 | 정지 | 깜빡 | 입, 차이 부분 빨간 테두리)으로 만들어 눈으로는 표시된 것만 봅니다.
- `tools/test.js`에 추가: `layers.js`의 id가 `shots.js`에 있는지, 파일이 있는지, 얼굴 bbox가 인물 bbox 안인지, 비율이 0~1인지, `timing.js`의 키가 `audio.js`에 있는지.

### 7. 짧은 영상 반복과 층 애니메이션 (선택, 몇 장면만)

- chapter-06(해 지는 장면)은 AI 영상이 아니라 **해를 층으로 떼어 `translateY`** 하면 그림을 한 픽셀도 바꾸지 않고 됩니다. 별·구름도 같은 방법으로 천천히 흘릴 수 있습니다.
- 철새를 타고 떠나는 09-f2, 별이 방울처럼 웃는 26-f4처럼 인물이 작거나 없는 장면만 AI 이미지→영상으로 3~5초 반복 영상을 만들어 `<video muted loop playsinline autoplay>`로 그림 대신 넣을 수 있습니다.
  인물 얼굴이 조금이라도 바뀐 영상은 쓰지 않습니다. `preserve-3d` 안의 `<video>`가 평면화되지 않는지 headless Chrome으로 먼저 확인합니다.

### 8. 확인과 넓히기

- `node tools/test.js` 통과, 6단계 도구가 2장 그림 전부를 통과.
- `prefers-reduced-motion`을 켠 상태에서 시차·깜빡임·입·흔들림이 멈추고 자막 강조만 남는지 확인.
- 층 경계에 테두리·후광이 보이지 않는지 클로즈업(cover×1.4, 1.7)에서 확인. 휴대폰 크기 창(390×844)에서도 확인.
- 2장이 승인되면 인물이 나오는 나머지 그림에 1~3단계를, 그 뒤 5단계를 돌립니다. 그림마다 Codex 편집이 3~5번 드니 `xargs -P 5`로 나눠 돌립니다.
  인물이 없는 그림은 하늘/땅만 나누거나 그대로 둡니다.
- 끝나면 `books/little-prince/images/README.md`와 `film/` 안의 설명, `README.md`의 파일 구조를 고칩니다.

## 권장 순서

① 1단계(입 곡선 + 단어 시각) → ② 2장 얼굴 스프라이트 + 입·깜빡임·자막 단어 강조 → ③ 2장 하늘/땅 층 + 카메라 드리프트 + 먼 층 흐림 → **사용자 확인** → ④ 목도리·숨쉬기(5단계) → ⑤ 자동 대조 도구(6단계)로 나머지 장 확장.

이 순서면 첫 시제품이 인물 뒤 인페인팅 없이 나오고, 가장 위험한 작업(인물 잘라 내기와 채우기)은 방향이 확인된 뒤에 합니다.
단어 단위 자막 강조는 영어 학습 사이트에서 립싱크보다 효과가 크니 립싱크와 같은 단계에 함께 넣습니다.

## 하지 말 것

- 3D 인물(three.js, Blender)은 다시 만들지 않습니다(2026-09-26 사용자 결정).
- 층을 위해 그림의 구도·인물 모습을 바꾸지 않습니다. 바뀐 편집은 버립니다.
- 생텍쥐페리 그림을 쓰거나 흉내 내지 않습니다.
- 층에 `translateZ`를 주지 않습니다(인물이 커지고 발이 뜸). SVG 필터를 매 프레임 바꾸지 않습니다(다시 래스터화).
